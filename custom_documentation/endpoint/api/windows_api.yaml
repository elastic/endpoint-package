overview:
  description: FILL ME IN
  name: FILL ME IN
identification:
  filter:
    event.dataset: endpoint.events.api
    event.module: endpoint
    host.os.type: windows
fields:
  endpoint:
  - '@timestamp'
