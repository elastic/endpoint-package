identification:
  filter:
    event.code: memory_signature
    event.dataset: endpoint.alerts
    event.module: endpoint
    host.os.type: linux
overview:
  description: FILL ME IN
  name: Linux Memory Threat Alert
fields:
  endpoint:
  - "@timestamp"

