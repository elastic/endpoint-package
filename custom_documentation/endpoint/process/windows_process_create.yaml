overview:
  description: FILL ME IN
  name: Windows Process Create
identification:
  filter:
    event.action: start
    event.dataset: endpoint.events.process
    event.module: endpoint
    host.os.type: windows
fields:
  endpoint:
  - '@timestamp'
