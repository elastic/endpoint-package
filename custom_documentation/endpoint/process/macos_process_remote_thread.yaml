overview:
  description: FILL ME IN
  name: macOS Process Exit
identification:
  filter:
    event.action: remote_thread
    event.dataset: endpoint.events.process
    event.module: endpoint
    host.os.type: macos
fields:
  endpoint:
  - '@timestamp'
