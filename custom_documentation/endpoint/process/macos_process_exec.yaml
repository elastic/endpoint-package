overview:
  description: FILL ME IN
  name: macOS Process Exec
identification:
  filter:
    event.action: exec
    event.dataset: endpoint.events.process
    event.module: endpoint
    host.os.type: macos
fields:
  endpoint:
  - '@timestamp'
