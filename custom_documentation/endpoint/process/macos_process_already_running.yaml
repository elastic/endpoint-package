overview:
  description: FILL ME IN
  name: macOS Already Running Process
identification:
  filter:
    event.dataset: endpoint.events.process
    event.module: endpoint
    event.type: info
    host.os.type: macos
fields:
  endpoint:
  - '@timestamp'
