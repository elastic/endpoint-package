overview:
  description: FILL ME IN
  name: FILL ME IN
identification:
  filter:
    event.action: mount
    event.dataset: endpoint.events.file
    event.module: endpoint
    host.os.type: macos
fields:
  endpoint:
  - '@timestamp'
