overview:
  name: Windows Already Running Process
  description: >
    This event is generated for a process that was already running before process collection was
    enabled.
identification:
  filter:
    event.action: already_running
    event.dataset: endpoint.events.process
    event.module: endpoint
    host.os.type: windows
  os:
  - windows
  data_stream: logs-endpoint.events.process-*
fields:
  endpoint:
  - '@timestamp'
