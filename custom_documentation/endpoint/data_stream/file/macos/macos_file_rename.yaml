overview:
  name: macOS File Rename
  description: >
    This event is generated when a file is renamed.
identification:
  filter:
    event.action: rename
    event.dataset: endpoint.events.file
    event.module: endpoint
    host.os.type: macos
  os:
  - macos
  data_stream: logs-endpoint.events.file-*
fields:
  endpoint:
  - '@timestamp'
