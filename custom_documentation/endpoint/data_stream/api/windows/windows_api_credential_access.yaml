overview:
  name: Windows API
  description: 'This event is generated when a process attempts to access priviledged
    credentials. '
identification:
  filter:
    event.dataset: endpoint.events.api
    event.module: endpoint
    event.type: access
    host.os.type: windows
  os:
  - windows
  data_stream: logs-endpoint.events.api-*
fields:
  endpoint:
  - '@timestamp'
