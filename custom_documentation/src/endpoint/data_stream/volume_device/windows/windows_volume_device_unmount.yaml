overview:
  name: Windows Device Unmount
  description: 'This event is generated when a device is unmounted.

    '
identification:
  filter:
    event.action: unmount
    event.dataset: endpoint.events.volume_device
    event.module: endpoint
    host.os.type: windows
  os:
  - windows
  data_stream: logs-endpoint.events.volume_device-*
fields:
  endpoint:
  - '@timestamp'
