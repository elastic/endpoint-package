---
- name: createdevice
  title: CreateDevice
  group: 2
  description: >
    Fields describing IoCreateDevice API calls.

  reusable:
    top_level: true
    expected:
      - { at: process.Ext.api, as: parameters }
  type: group

  fields:
    - name: device
      level: custom
      type: keyword
      description: >
        The name of the device object.
      example: "\\Device\\NPCAP"

    - name: driver
      level: custom
      type: keyword
      description: >
        The name of the driver object.
      example: "\\Driver\\npcap"