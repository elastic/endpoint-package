---
# NOTE this file is temporary and once ECS merges in the data_stream fields into core we should remove these
- name: data_stream
  title: data_stream
  group: 2
  short: Fields describing the new indexing strategy
  description: >
    Fields describing the new indexing strategy <type>-<dataset>-<namespace>
  type: group
  fields:
    - name: type
      level: custom
      type: constant_keyword
      description: >
        Data stream type.

    - name: dataset
      level: custom
      type: constant_keyword
      description: >
        Data stream dataset name.

    - name: namespace
      level: custom
      type: constant_keyword
      description: >
        Data stream namespace.
