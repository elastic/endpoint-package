---
- name: Events
  title: Events
  group: 2
  short: Fields to capture events array in rule detection alerts
  description: >
   Events array contains endpoint events which triggered the rule detection

  type: group
  fields: []
